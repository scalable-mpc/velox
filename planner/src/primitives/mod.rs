//! The two building blocks the operations are made of: a random value with
//! its bits, and the bitwise less-than schedule. Neither knows about
//! operations, steps or the engine.

pub mod carry_tree;
pub mod edabit;
