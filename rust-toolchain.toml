[toolchain]
# commonware 2026.7 (pulled in by `avid_ab` for Reed-Solomon coding) requires
# rustc >= 1.89 via its `crc-fast` dependency. The rest of the workspace builds
# unchanged on this toolchain.
channel = "1.97.1"
